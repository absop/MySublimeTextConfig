{
    "cmd": ["${file}"],
    "file_regex": "^(..[^:]*):([0-9]+):?([0-9]+)?:? (.*)$",
    "working_dir": "${file_path}",
    "encoding": "gbk",
    "selector": "source.dosbatch",

    "variants": [
        {
            "name": "Run(CMD)",
            "cmd": ["${file}"]
        }
    ]
}

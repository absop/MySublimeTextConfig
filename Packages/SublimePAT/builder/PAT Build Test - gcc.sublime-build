{
    "target": "pat_build_test",
    "selector": "source.c",
    "shell": true,
    "compile_cmd": ["gcc", "${file}", "-o", "${cache_path}/${file_base_name}"],
    "test_cmd": ["${cache_path}/${file_base_name}"]
}
